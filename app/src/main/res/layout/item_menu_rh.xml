<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:layout_marginBottom="15dp"
    app:cardCornerRadius="15dp"
    app:cardElevation="3dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="15dp">

        <TextView
            android:id="@+id/txtMenuTitulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:text="Benefícios"
            android:textColor="#000000"
            android:textSize="28sp" />

        <View
            android:id="@+id/bgIconeCirculo"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/circle_background_gray" />

        <ImageView
            android:id="@+id/imgMenuIcone"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:layout_alignTop="@id/bgIconeCirculo"
            android:layout_alignBottom="@id/bgIconeCirculo"
            android:layout_alignStart="@id/bgIconeCirculo"
            android:layout_alignEnd="@id/bgIconeCirculo"
            android:padding="5dp"
            android:src="@drawable/beneficios" />

    </RelativeLayout>
</androidx.cardview.widget.CardView>