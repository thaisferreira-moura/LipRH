<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FDFDFD"
        android:padding="16dp">

        <ImageView
            android:id="@+id/imgIconeExame"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:src="@drawable/ic_exame"
            android:contentDescription="Ícone do exame" />

        <TextView
            android:id="@+id/txtNomeExame"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_toEndOf="@id/imgIconeExame"
            android:fontFamily="sans-serif-black"
            android:text="Hemograma"
            android:textColor="#A52A2A"
            android:textSize="18sp" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/txtNomeExame"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="16dp"
            android:orientation="horizontal">

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnAnexar"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:background="@drawable/shape_button_exame"
                android:drawableStart="@drawable/ic_clipe"
                android:drawablePadding="8dp"
                android:fontFamily="sans-serif-medium"
                android:paddingHorizontal="16dp"
                android:text="anexar exame"
                android:textAllCaps="false"
                android:textColor="#A52A2A"
                android:textSize="13sp" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnBaixarGuia"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/shape_button_exame"
                android:drawableStart="@drawable/ic_file_download"
                android:drawablePadding="8dp"
                android:fontFamily="sans-serif-medium"
                android:paddingHorizontal="16dp"
                android:text="baixar guia"
                android:textAllCaps="false"
                android:textColor="#A52A2A"
                android:textSize="13sp" />

        </LinearLayout>
    </RelativeLayout>
</androidx.cardview.widget.CardView>